<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
</head>
<body>
    <h1>URL shortner</h1>
    <% if (locals.status == 'same url') { %>
        <p>Your previously entered url has been sorted</p>
        <p>localhost:8001/url/<%= locals.sortid %></p>
    <% } %>
    <% if (locals.id) { %>
        <p>previously Generates url is: localhost:8001/url/<%= id %></p>
    <% } %>
    <div>
        <form method="POST" action="/url">
            <label for="">Enter you orginal url</label>
            <input type="text" name="url" placeholder="example link"/>
            <button type="submit">Generate</button>
        </form>
    </div>
    <div>
        <% if (locals.urls) { %>
            <table>
                <thead>
                    <th>S.No</th>
                    <th>shortedUrl</th>
                    <th>OriginalUrl</th>
                    <th>Totlaclicks</th>
                </thead>
                <tbody>
                    <% urls.forEach((url , index) => { %>
                       <tr>
                            <td><%= index + 1 %></td>
                            <td><%= url.sortid %></td>
                            <td><%= url.redirectul %></td>
                            <td><%= url.visithistory.length %></td>
                       </tr> 
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>